apiVersion: v1
kind: Service
metadata:
  name: {{ include "hashpad.fullname" . }}
  labels:
    {{- include "hashpad.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hashpad.selectorLabels" . | nindent 4 }}
